{
  "name": "@sudoku-platform/shared",
  "version": "1.0.0",
  "description": "Shared core logic and interfaces for Sudoku platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.1.0",
    "vitest": "^0.34.0"
  },
  "files": [
    "dist"
  ]
}